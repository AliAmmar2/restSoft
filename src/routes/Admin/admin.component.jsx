import React from 'react';
import RestMenu from '../restMenu/restMenu.component';

const RestMenuAdmin = () => {
    return (
        <RestMenu isAdmin={true} />
    );
}

export default RestMenuAdmin;